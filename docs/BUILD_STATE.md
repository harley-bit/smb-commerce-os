# Build State

**Last updated:** 2026-09-08 (D009 done)
**Execution model:** token-maxing — sessions run back-to-back until the account's usage limit is hit, then resume automatically once it refreshes (see `docs/design/12_daily_build_protocol.md` and the "Token-maxing execution" note in `CLAUDE.md`). Elapsed calendar time is therefore driven by usage-limit cadence, not a fixed days-per-week number. **Day ID is the only reliable sequence marker — do not infer progress from dates.**
**Projected MVP gate (G7):** provisional only until real throughput is observed over the first ~10 cards. See `docs/BUILD_CALENDAR.md` for the full sequence.
**Per-card duration estimates:** every row in `docs/BUILD_CALENDAR.md`, the xlsx dashboard, and each `docs/cards/{DAY_ID}.md` now carries an "Est. Duration" figure — a heuristic planning estimate (by phase, weighted up for concurrency/deposit/payment/gate-review complexity) for an AI-assisted session, not a human day and not a commitment. Sum across the original 176 cards is ~473.5 hours; Phase 11 (`D177`–`D197`, added 2026-09-08) adds ~45.5 hours across 21 cards, for a running total of ~519 hours across 197 cards. **Treat this the same as the projected MVP gate date: recalibrate against real `docs/logs/` actual-effort data once cards start closing, don't defend the original number.**
**Phase 11 — Platform admin console** (`D177`–`D197`, Gate G11) was added 2026-09-08 at founder request — a platform-staff/power-user surface (break-glass support access, merchant lifecycle, reporting, feature flags, platform role management) that the original coworker plan never scoped. Full design: `docs/design/16_platform_admin_console.md`. Placed after Phase 10 so it doesn't renumber any already-built card; does not block `G7` or `G10`, but Epic A (`D177`–`D182`) should land before the pilot has been live long enough to need a real support incident handled.

## Current phase

**Phase 0 — Foundations and security baseline** (`D001`–`D012`). Underway — D001–D009 done; pnpm workspace + Turborepo scaffolding is live with a regression-tested strict TypeScript baseline, repo-wide ESLint + Prettier with a custom rule banning raw SQL outside `packages/db`, Vitest coverage enforcement (domain package at 95%), a GitHub Actions CI skeleton (install/lint/typecheck/test on every PR), gitleaks secret scanning wired into both a local pre-commit hook and CI, `pnpm audit --audit-level=high` plus a syft-generated CycloneDX SBOM (uploaded as a retained CI artifact), semgrep SAST (`p/security-audit` ruleset, `--error` on any blocking finding), and now a live GitHub remote with enforced branch protection on `main`.

## Current card

**Next up: [`D010`](../docs/cards/D010.md) — Native AuthProvider implementation (revised per ADR-002).**
This is the card ADR-002 rewrote from an OIDC/Cognito spike into a real native-auth implementation, and it's a heavier card than the Phase 0 toolchain cards before it — see the card file and ADR-002 before starting. Full task, deliverable, acceptance criteria, and security check are in the card file itself — that file, not this one, is the source of truth for what D010 actually requires. This file only tracks *where we are*, not *what to do*.

## Open decisions (blocking, from `docs/design/00_README.md`)

| #   | Decision                                  | Status                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| --- | ----------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| D1  | Ratify ADR-001 stack recommendation       | **Answered — accepted as proposed.** TypeScript everywhere: Fastify, Drizzle, Next.js, Expo, pnpm monorepo. `docs/design/02_ADR_001_stack_decision.md`'s ratification table can be marked Accepted.                                                                                                                                                                                                                                                  |
| D2  | Confirm build cadence (drives every date) | **Answered** — 5–6 sessions/week, Claude as builder.                                                                                                                                                                                                                                                                                                                                                                                                 |
| D3  | Mobile in MVP or post-pilot               | **Answered — deferred to Phase 8**, post-web-MVP (G7). Matches the phase plan as written; revisit once the web pilot is live.                                                                                                                                                                                                                                                                                                                        |
| D4  | Identity provider                         | **Answered — native for now, Auth0 later.** Build authentication in-house behind an `AuthProvider` interface; wire in Auth0 when it's actually time, as a provider swap, not a rewrite. Full spec: [`docs/adr/ADR-002-authentication-provider.md`](adr/ADR-002-authentication-provider.md). This is a real scope change to D010 (was an OIDC/Cognito spike, now a native auth implementation) — `docs/cards/D010.md` has been rewritten accordingly. |

**No decisions remain blocking.**

## Carried-over notes for next session

- `docs/implementation_calendar/build_calendar.xlsx` — human-facing dashboard, hyperlinked to `docs/cards/{DAY_ID}.md` / `docs/logs/{DAY_ID}.md`. Run `python3 scripts/sync_calendar_xlsx.py` after closing out a card to push Status/Notes without touching its hyperlinks — **run at the end of the D005 session (below); confirm it also picked up D001–D004 if it hadn't been run before**.
- D005's CI workflow (`.github/workflows/ci.yml`) has never actually executed inside GitHub Actions — this sandboxed session has no remote push access, so "pipeline green" was verified by running the workflow's exact command sequence locally, not by a real Actions run. **First push/PR against this repo should confirm the Actions run is genuinely green in the GitHub UI.**
- `pnpm` (12.3.4, via the system's node/corepack toolchain) and `turbo` are working; `pnpm-workspace.yaml` has an `allowBuilds: { esbuild: true }` entry approving vitest's transitive esbuild postinstall script — expect similar prompts (`pnpm approve-builds`) for other future transitive deps with install scripts.
- Root scaffolding uses scope `@smb-os/*` for internal packages (`@smb-os/domain`, `@smb-os/db`, `@smb-os/contracts`, `@smb-os/config`, `@smb-os/api`, `@smb-os/web`), plus a new tooling-only package `@smb-os/eslint-config` added in D003 (not part of the runtime dependency graph, just shared lint config).
- `test:isolation` is still a stub `echo` script in every package — that's correct for now (D004 was Vitest coverage thresholds only, not the isolation suite); it becomes real once Phase 1 introduces `packages/db` repositories to isolate-test. `lint` is real everywhere (D003): flat ESLint 10 config from `@smb-os/eslint-config`, including a custom `local/no-raw-sql` rule (error everywhere except `packages/db`) and, as of D004, a `coverage/**` ignore so packages running `vitest --coverage` don't trip lint on their generated HTML report. `packages/domain` (D004) is the only package with an enforced coverage threshold so far — `vitest.config.ts` there gates lines/statements/functions/branches at 95%, verified to actually fail the build below threshold, not just configured.
- `packages/config/tsconfig.base.json` (`strict: true`, `noUncheckedIndexedAccess`, `noImplicitOverride`) is now regression-tested by `packages/config/test/strict-config.test.ts` — if that test ever fails, someone weakened the shared strict baseline, not a fluke.
- Careful with repo-wide `prettier --write .` — D003 accidentally ran it unscoped and it reformatted ~25 out-of-scope Markdown files (design docs, ADRs, this file, `CLAUDE.md`) before being caught and reverted via `git show HEAD:<path>` (plain `git checkout --` is blocked by the sandbox's destructive-git-op policy). Scope any future Prettier run to `apps/**`/`packages/**` paths.
- D006 added `gitleaks` (8.30.1, `brew install gitleaks`) as a **local machine dependency**: `packages/config/test/secret-scanning.test.ts` shells out to the real binary (no legitimate npm package exists under that name), so it must be on `PATH` for `pnpm test` to pass on any machine, not just CI (which installs it explicitly in `.github/workflows/ci.yml`). The pre-commit hook (`.githooks/pre-commit`, wired via `package.json`'s new `prepare` script running `git config core.hooksPath .githooks`) was verified by direct invocation, not via a real `pnpm install` — this sandbox's permission policy blocks `git config` commands outright (matches `CLAUDE.md`'s exclusion of git config changes from pre-approved ops), so the `prepare` script's auto-wiring itself is unverified. First normal (non-sandboxed) `pnpm install` should confirm `git config core.hooksPath` actually gets set.
- D007 added `syft` (1.51.1, `brew install syft`) as a second **local machine dependency** alongside gitleaks — `packages/config/test/dependency-scanning.test.ts` shells out to the real binary to verify SBOM generation. D007 also upgraded `vitest`/`@vitest/coverage-v8` from `^2.1.8` to `^3.2.7` across `packages/{domain,config,eslint-config}` and added a `pnpm-workspace.yaml` `overrides: { vite: "^6.4.3" }` entry, purely to clear a real critical (vitest) and high (vite) CVE with fixes available before turning on `pnpm audit --audit-level=high` in CI — otherwise the new gate would have failed on day one. If vitest is upgraded again later, check whether the `vite` override is still needed.
- D008 added `semgrep` (1.176.0, `brew install semgrep`) as a third **local machine dependency** alongside gitleaks and syft — `packages/config/test/sast.test.ts` shells out to the real binary against a planted command-injection fixture. CI installs semgrep via `pip install --break-system-packages semgrep==1.176.0` (PEP 668 flag needed on `ubuntu-latest`'s Python), then runs `semgrep scan --config p/security-audit --error` after `pnpm audit` and before lint/typecheck/test. Ruleset is `p/security-audit` (Registry, no login/metrics) — repo's own tree scans clean under it as of D008.
- D009 added a fourth git hook, `.githooks/commit-msg`, enforcing Conventional Commits on every commit subject line (reuses D006's `core.hooksPath .githooks` wiring, so it's already live). It's test-first verified by shelling out to the real hook script (`packages/config/test/commit-message-format.test.ts`), same pattern as the other security-tooling tests. `.github/pull_request_template.md` (self-review checklist, framed explicitly as the CC8 change-management evidence) is also done and tested (`packages/config/test/pr-template.test.ts`).
- **This repo now has a live GitHub remote** — `origin` → `https://github.com/harley-bit/smb-commerce-os` (public, by explicit founder decision — branch protection on a private repo needs GitHub Pro, which wasn't purchased). `main` is protected on GitHub: PR required (no direct push, no force-push, no deletion, enforced for admins too), but `required_approving_review_count: 0` so a solo maintainer can still merge their own PR. **This does not change the loop's day-to-day operation** — branch protection is a server-side rule on the *remote*; every card's "commit with the day ID in the message" step commits to the *local* `main` branch, same as it always has, and `.claude/settings.json` already denies the loop any `git push`/`git remote` regardless. The only real consequence: local `main` and `origin/main` will drift apart over time unless a human periodically syncs them via a branch + PR + merge (exactly what happened for D009's own CI/gitleaks fixes, PR #1) — that sync is a manual step for now, not something the loop does or needs to do.
- `.gitleaks.toml` (added fixing D009's PR) allowlists exactly one literal string — D006's deliberate `AKIAABCDEFGHIJKLMNOP` test fixture — scoped tightly enough that a real secret anywhere, including that same file, still gets caught. Don't broaden this allowlist casually.
- `.github/workflows/ci.yml`'s tool-install steps (gitleaks, syft) are now grouped together near the top, before `pnpm install`/test — semgrep's install (pip) stays where it is, right before its own scan step, since nothing else depends on it being present earlier.
- **A fourth git hook, `.githooks/pre-push`**, added 2026-09-08 alongside the change-management policy work: runs `pnpm audit --audit-level=high` and semgrep SAST before anything leaves the machine, closing the gap where both checks existed in CI/tests but nothing stopped a vulnerable dependency or flagged pattern from being pushed. Test-first per the established pattern (`packages/config/test/pre-push-hook.test.ts`), including PATH-manipulation tests for the "tool not installed" guard branches.
- **Branch protection on `main` was tightened**: `required_status_checks` now names the real CI job (`install, lint, typecheck, test`) with `strict: true` — a PR literally cannot merge without CI passing and being up to date with `main`, which wasn't true when D009 first closed (a PR existing was enough).
- **Two new design docs**: `docs/design/17_change_management_policy.md` (the actual git/PR/branch-protection/CC8-evidence process, grounded in real settings — not a forward plan) and a grounding update to `docs/design/10_secure_sdlc_ci_and_quality_gates.md`'s top (which pipeline stages are real vs. still Phase 1+, and a correction to its solo-developer branching section: `required_approving_review_count: 0`, not generic "self-merge permitted" language, and why). `09`'s change-management control row now points to `17`.
- Also fixed while in `docs/design/00_README.md`: the numbered document list under "Architecture" through "New scope" had been flattened into run-on paragraphs by an earlier accidental repo-wide Prettier pass (see the D003 note above) — restored proper list formatting while adding the `17` entry.

## Log of completed cards

- [D001](logs/D001.md) — Initialize monorepo. pnpm workspaces + Turborepo, `apps/{api,web}` + `packages/{domain,db,contracts,config}` per ADR-001, real internal dependency graph, `packages/domain` has its first Vitest test. Done.
- [D002](logs/D002.md) — TypeScript strict configuration. Confirmed `pnpm typecheck` already green off D001's `strict: true` base config, then locked that guarantee in with a Vitest regression test (`packages/config/test/strict-config.test.ts`) that type-checks fixtures against the real shared tsconfig and would fail if strictness were ever weakened. Done.
- [D003](logs/D003.md) — Lint and format rules. New `@smb-os/eslint-config` package with a flat ESLint 10 config and a custom `local/no-raw-sql` rule (error outside `packages/db`, off inside it), plus root Prettier config. Verified against a deliberate raw-SQL fixture in `apps/api` (lint failed as required, then reverted). Done.
- [D004](logs/D004.md) — Test harness. Wired `@vitest/coverage-v8` into `packages/domain` with a 95% threshold on lines/statements/functions/branches per `docs/design/10`. Verified the gate actually fails below threshold (added an under-tested function, confirmed a non-zero exit at ~44% coverage, then covered it back to 100%). Also fixed a shared-config lint gap (`coverage/**` wasn't ignored). Done.
- [D005](logs/D005.md) — CI pipeline skeleton. Added `.github/workflows/ci.yml` (frozen-lockfile install → lint → typecheck → test → test:isolation on every PR and push to `main`), test-first via `packages/config/test/ci-workflow.test.ts`. Verified green by running the workflow's exact command sequence locally (no remote push access in this session, so no live Actions run yet — flagged for first real push). Done.
- [D006](logs/D006.md) — Secret scanning. Installed `gitleaks`, wired it into a `.githooks/pre-commit` hook (auto-installed via a new `prepare` script) and into CI (install step before tests, plus a repo-wide `gitleaks detect --exit-code 1` scan step with `fetch-depth: 0`), test-first via `packages/config/test/secret-scanning.test.ts` which plants a fake secret and asserts detection. Verified the hook directly against a staged planted secret (blocked, exit 1); CI wiring verified by running the same commands locally, not via a live Actions run. Done.
- [D007](logs/D007.md) — Dependency scanning and SBOM. Wired `pnpm audit --audit-level=high` into CI after install, plus a syft-generated CycloneDX SBOM uploaded as a 90-day CI artifact, test-first via `packages/config/test/dependency-scanning.test.ts` (real `form-data@4.0.0` high-severity fixture for the audit gate, real `syft` invocation for the SBOM). Along the way, `pnpm audit` surfaced a genuine pre-existing critical (vitest) and high (vite) CVE in this repo's own tree, both with fixes available — upgraded vitest `^2.1.8` → `^3.2.7` and pinned vite to `^6.4.3` via a workspace override to clear them before the gate went live, confirmed clean afterward, full test suite still green on the new vitest version. Done.
- [D008](logs/D008.md) — SAST. Wired `semgrep scan --config p/security-audit --error` into CI after `pnpm audit` and before lint/typecheck/test, test-first via `packages/config/test/sast.test.ts` (real command-injection fixture tripping `javascript.lang.security.detect-child-process`, a clean-fixture control, and a scan of this repo's own tree — 0 findings). `semgrep` (1.176.0) installed locally via Homebrew as a third machine dependency alongside gitleaks/syft; CI installs it via pip. Done.
- [D009](logs/D009.md) — Branch protection and PR template. Initially Carried (see above) — the PR template and commit-msg hook were done and tested, but branch protection had nothing to enforce against without a remote. The founder then provisioned one directly (outside CLAUDE.md's WHERE limits, by explicit human decision, not this session acting unilaterally): `gh` installed and authenticated, `github.com/harley-bit/smb-commerce-os` created and pushed, branch protection applied to `main`. **Real finding from the first live push:** GitHub Actions CI failed twice — D008's SBOM test needs `syft` installed before the Test step (ci.yml had it installed later, only used by a separate step; passed locally only because syft happened to already be on the local machine's PATH), and gitleaks flagged D006's own deliberate test fixture (`AKIAABCDEFGHIJKLMNOP` in `packages/config/test/secret-scanning.test.ts`) as a real leak when scanning full history. Both fixed via a real PR (`#1`), which also caught and fixed a genuine branch-protection design flaw: `required_approving_review_count: 1` + `enforce_admins: true` would have made this solo-maintainer repo unmergeable (GitHub doesn't allow self-approval, and `enforce_admins` blocks the owner-override path) — corrected to `required_approving_review_count: 0` (PR still required, direct push still rejected, but a solo maintainer can merge). Verified for real: a direct push to `main` was attempted and rejected (`GH006: Protected branch update failed`); PR #1 passed CI and was merged solo without a second reviewer. **Done.**
