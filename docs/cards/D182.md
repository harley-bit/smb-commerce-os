# D182 — Support console: merchant/order/reservation lookup (read-only)

**Build calendar:** [`docs/BUILD_CALENDAR.md`](../BUILD_CALENDAR.md#d182) · **Phase 11 — Platform Admin Console** · Epic: Platform auth & break-glass
**Log for this card:** `docs/logs/D182.md` (created when this card is run)
**Gate this card contributes to:** —
**Estimated build duration (AI-assisted session):** 2.0h — planning estimate, not a commitment; recalibrate against `docs/logs/D182.md` actual effort once run.

## Task

Support console: merchant/order/reservation lookup (read-only)

## Deliverable

Search by merchant ID/slug or merchant-user email; loads merchant orders/reservations/listings only inside an active grant

## Acceptance criteria

A lookup attempted without an active grant is refused; a lookup during a valid grant succeeds and is audit-logged with the grant ID

## Security check

Confirm search never does free-text matching across PII fields without a specific identifier

## Prompt to run this card

Follow `CLAUDE.md` exactly. Read `docs/BUILD_STATE.md` first to confirm this is in fact the current card and that the tree is green. Read `docs/design/16_platform_admin_console.md` for the full context this card sits in before implementing. Then:

1. Write the failing test first (non-negotiable for domain, reservations, inventory, money, auth code — and for anything in Phase 11 touching `support_access_grants` or platform roles).
2. Implement the smallest change that satisfies the acceptance criteria above.
3. Run the local gate (lint, typecheck, test, isolation suite where applicable).
4. Answer the security check above explicitly in the log entry.
5. Commit with `[D182]` in the message.
6. Write `docs/logs/D182.md` (use `docs/logs/TEMPLATE.md`), update `docs/BUILD_STATE.md` and this card's Status in `docs/BUILD_CALENDAR.md`.

If this card cannot be finished and verified in this run, mark it **Carried** — do not mark it Done.
