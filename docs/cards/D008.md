# D008 — SAST

**Build calendar:** [`docs/BUILD_CALENDAR.md`](../BUILD_CALENDAR.md#d008) · **Phase 0 — Foundations & Security Baseline** · Epic: CI/CD
**Log for this card:** `docs/logs/D008.md` (created when this card is run)
**Gate this card contributes to:** —
**Estimated build duration (AI-assisted session):** 1.0h — planning estimate, not a commitment; recalibrate against `docs/logs/D008.md` actual effort once run.

## Task

SAST

## Deliverable

CodeQL or Semgrep in CI

## Acceptance criteria

Build fails on high/critical finding

## Security check

Static analysis before any application code exists

## Prompt to run this card

Follow `CLAUDE.md` exactly. Read `docs/BUILD_STATE.md` first to confirm this is in fact the current card and that the tree is green. Then:

1. Write the failing test first (non-negotiable for domain, reservations, inventory, money, auth code).
2. Implement the smallest change that satisfies the acceptance criteria above.
3. Run the local gate (lint, typecheck, test, isolation suite where applicable).
4. Answer the security check above explicitly in the log entry.
5. Commit with `[D008]` in the message.
6. Write `docs/logs/D008.md` (use `docs/logs/TEMPLATE.md`), update `docs/BUILD_STATE.md` and this card's Status in `docs/BUILD_CALENDAR.md`.

If this card cannot be finished and verified in this run, mark it **Carried** — do not mark it Done.
