# D186 — Support action audit trail + merchant-visible support-access notice

**Build calendar:** [`docs/BUILD_CALENDAR.md`](../BUILD_CALENDAR.md#d186) · **Phase 11 — Platform Admin Console** · Epic: Support actions
**Log for this card:** `docs/logs/D186.md` (created when this card is run)
**Gate this card contributes to:** —
**Estimated build duration (AI-assisted session):** 2.0h — planning estimate, not a commitment; recalibrate against `docs/logs/D186.md` actual effort once run.

## Task

Support action audit trail + merchant-visible support-access notice

## Deliverable

Every Epic B action's audit row is queryable by grant ID; small addition to the merchant dashboard (Phase 6) surfacing when platform support accessed the account, when, and why

## Acceptance criteria

A merchant OWNER can see a real support-access event (grant reason and timestamp) in their own dashboard within one refresh

## Security check

Confirm the merchant-visible notice cannot be suppressed or delayed by the platform user who created the grant

## Prompt to run this card

Follow `CLAUDE.md` exactly. Read `docs/BUILD_STATE.md` first to confirm this is in fact the current card and that the tree is green. Read `docs/design/16_platform_admin_console.md` for the full context this card sits in before implementing. Then:

1. Write the failing test first (non-negotiable for domain, reservations, inventory, money, auth code — and for anything in Phase 11 touching `support_access_grants` or platform roles).
2. Implement the smallest change that satisfies the acceptance criteria above.
3. Run the local gate (lint, typecheck, test, isolation suite where applicable).
4. Answer the security check above explicitly in the log entry.
5. Commit with `[D186]` in the message.
6. Write `docs/logs/D186.md` (use `docs/logs/TEMPLATE.md`), update `docs/BUILD_STATE.md` and this card's Status in `docs/BUILD_CALENDAR.md`.

If this card cannot be finished and verified in this run, mark it **Carried** — do not mark it Done.
