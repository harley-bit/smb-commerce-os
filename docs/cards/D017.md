# D017 — TenantContext and repository base

**Build calendar:** [`docs/BUILD_CALENDAR.md`](../BUILD_CALENDAR.md#d017) · **Phase 1 — Domain Model & Database** · Epic: Tenancy
**Log for this card:** `docs/logs/D017.md` (created when this card is run)
**Gate this card contributes to:** —

## Task

TenantContext and repository base

## Deliverable

TenantContext type; base class injecting merchant_id predicate

## Acceptance criteria

Repository method without ctx is a compile error

## Security check

Control #1 — tenant isolation, layer 2 (type system)

## Prompt to run this card

Follow `CLAUDE.md` exactly. Read `docs/BUILD_STATE.md` first to confirm this is in fact the current card and that the tree is green. Then:

1. Write the failing test first (non-negotiable for domain, reservations, inventory, money, auth code).
2. Implement the smallest change that satisfies the acceptance criteria above.
3. Run the local gate (lint, typecheck, test, isolation suite where applicable).
4. Answer the security check above explicitly in the log entry.
5. Commit with `[D017]` in the message.
6. Write `docs/logs/D017.md` (use `docs/logs/TEMPLATE.md`), update `docs/BUILD_STATE.md` and this card's Status in `docs/BUILD_CALENDAR.md`.

If this card cannot be finished and verified in this run, mark it **Carried** — do not mark it Done.
