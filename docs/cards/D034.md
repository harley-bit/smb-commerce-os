# D034 — Contracts package

**Build calendar:** [`docs/BUILD_CALENDAR.md`](../BUILD_CALENDAR.md#d034) · **Phase 2 — Catalog & Product Inventory** · Epic: Domain + API foundation
**Log for this card:** `docs/logs/D034.md` (created when this card is run)
**Gate this card contributes to:** —
**Estimated build duration (AI-assisted session):** 1.5h — planning estimate, not a commitment; recalibrate against `docs/logs/D034.md` actual effort once run.

## Task

Contracts package

## Deliverable

Zod schemas for listings and variants; generated types

## Acceptance criteria

Web and mobile compile against generated types

## Security check

One validation definition, enforced everywhere

## Prompt to run this card

Follow `CLAUDE.md` exactly. Read `docs/BUILD_STATE.md` first to confirm this is in fact the current card and that the tree is green. Then:

1. Write the failing test first (non-negotiable for domain, reservations, inventory, money, auth code).
2. Implement the smallest change that satisfies the acceptance criteria above.
3. Run the local gate (lint, typecheck, test, isolation suite where applicable).
4. Answer the security check above explicitly in the log entry.
5. Commit with `[D034]` in the message.
6. Write `docs/logs/D034.md` (use `docs/logs/TEMPLATE.md`), update `docs/BUILD_STATE.md` and this card's Status in `docs/BUILD_CALENDAR.md`.

If this card cannot be finished and verified in this run, mark it **Carried** — do not mark it Done.
