# D189 — Merchant onboard/offboard admin view

**Build calendar:** [`docs/BUILD_CALENDAR.md`](../BUILD_CALENDAR.md#d189) · **Phase 11 — Platform Admin Console** · Epic: Merchant lifecycle
**Log for this card:** `docs/logs/D189.md` (created when this card is run)
**Gate this card contributes to:** —
**Estimated build duration (AI-assisted session):** 2.5h — planning estimate, not a commitment; recalibrate against `docs/logs/D189.md` actual effort once run.

## Task

Merchant onboard/offboard admin view

## Deliverable

Create a merchant record and initial OWNER invitation from apps/admin; offboard triggers the existing retention rules (08), not a new policy

## Acceptance criteria

A newly onboarded merchant can complete OWNER signup from the invitation; an offboarded merchant's data handling matches 08 exactly

## Security check

Confirm offboarding does not delete financial/audit records — only catalog objects are archived, per 03's soft-delete rule

## Prompt to run this card

Follow `CLAUDE.md` exactly. Read `docs/BUILD_STATE.md` first to confirm this is in fact the current card and that the tree is green. Read `docs/design/16_platform_admin_console.md` for the full context this card sits in before implementing. Then:

1. Write the failing test first (non-negotiable for domain, reservations, inventory, money, auth code — and for anything in Phase 11 touching `support_access_grants` or platform roles).
2. Implement the smallest change that satisfies the acceptance criteria above.
3. Run the local gate (lint, typecheck, test, isolation suite where applicable).
4. Answer the security check above explicitly in the log entry.
5. Commit with `[D189]` in the message.
6. Write `docs/logs/D189.md` (use `docs/logs/TEMPLATE.md`), update `docs/BUILD_STATE.md` and this card's Status in `docs/BUILD_CALENDAR.md`.

If this card cannot be finished and verified in this run, mark it **Carried** — do not mark it Done.
