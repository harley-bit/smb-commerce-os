# D085 — Media retention job

**Build calendar:** [`docs/BUILD_CALENDAR.md`](../BUILD_CALENDAR.md#d085) · **Phase 4 — Rentals, Deposits & Returns** · Epic: Exceptions
**Log for this card:** `docs/logs/D085.md` (created when this card is run)
**Gate this card contributes to:** —

## Task

Media retention job

## Deliverable

90-day / 2-year retention with dry-run and deletion audit

## Acceptance criteria

Dry run lists correctly; deletion writes an audit record

## Security check

Retention set when the feature ships, not deferred

## Prompt to run this card

Follow `CLAUDE.md` exactly. Read `docs/BUILD_STATE.md` first to confirm this is in fact the current card and that the tree is green. Then:

1. Write the failing test first (non-negotiable for domain, reservations, inventory, money, auth code).
2. Implement the smallest change that satisfies the acceptance criteria above.
3. Run the local gate (lint, typecheck, test, isolation suite where applicable).
4. Answer the security check above explicitly in the log entry.
5. Commit with `[D085]` in the message.
6. Write `docs/logs/D085.md` (use `docs/logs/TEMPLATE.md`), update `docs/BUILD_STATE.md` and this card's Status in `docs/BUILD_CALENDAR.md`.

If this card cannot be finished and verified in this run, mark it **Carried** — do not mark it Done.
