# D187 — Merchant suspend workflow

**Build calendar:** [`docs/BUILD_CALENDAR.md`](../BUILD_CALENDAR.md#d187) · **Phase 11 — Platform Admin Console** · Epic: Merchant lifecycle
**Log for this card:** `docs/logs/D187.md` (created when this card is run)
**Gate this card contributes to:** —
**Estimated build duration (AI-assisted session):** 2.0h — planning estimate, not a commitment; recalibrate against `docs/logs/D187.md` actual effort once run.

## Task

Merchant suspend workflow

## Deliverable

PLATFORM_ADMIN+ suspends a merchant: immediate storefront takedown, mandatory reason, notifies all merchant OWNERs

## Acceptance criteria

A suspended merchant's storefront returns unavailable within seconds; OWNERs receive the notification; action is audit-logged

## Security check

Confirm a suspended merchant's existing customer sessions cannot complete a new checkout

## Prompt to run this card

Follow `CLAUDE.md` exactly. Read `docs/BUILD_STATE.md` first to confirm this is in fact the current card and that the tree is green. Read `docs/design/16_platform_admin_console.md` for the full context this card sits in before implementing. Then:

1. Write the failing test first (non-negotiable for domain, reservations, inventory, money, auth code — and for anything in Phase 11 touching `support_access_grants` or platform roles).
2. Implement the smallest change that satisfies the acceptance criteria above.
3. Run the local gate (lint, typecheck, test, isolation suite where applicable).
4. Answer the security check above explicitly in the log entry.
5. Commit with `[D187]` in the message.
6. Write `docs/logs/D187.md` (use `docs/logs/TEMPLATE.md`), update `docs/BUILD_STATE.md` and this card's Status in `docs/BUILD_CALENDAR.md`.

If this card cannot be finished and verified in this run, mark it **Carried** — do not mark it Done.
