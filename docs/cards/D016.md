# D016 — Core utilities

**Build calendar:** [`docs/BUILD_CALENDAR.md`](../BUILD_CALENDAR.md#d016) · **Phase 1 — Domain Model & Database** · Epic: Database foundation
**Log for this card:** `docs/logs/D016.md` (created when this card is run)
**Gate this card contributes to:** —
**Estimated build duration (AI-assisted session):** 2.0h — planning estimate, not a commitment; recalibrate against `docs/logs/D016.md` actual effort once run.

## Task

Core utilities

## Deliverable

UUIDv7 generator; Money minor-unit value object; UTC time helpers

## Acceptance criteria

Property tests: no float money; all timestamps UTC

## Security check

Money as integer minor units — floats are unacceptable for money

## Prompt to run this card

Follow `CLAUDE.md` exactly. Read `docs/BUILD_STATE.md` first to confirm this is in fact the current card and that the tree is green. Then:

1. Write the failing test first (non-negotiable for domain, reservations, inventory, money, auth code).
2. Implement the smallest change that satisfies the acceptance criteria above.
3. Run the local gate (lint, typecheck, test, isolation suite where applicable).
4. Answer the security check above explicitly in the log entry.
5. Commit with `[D016]` in the message.
6. Write `docs/logs/D016.md` (use `docs/logs/TEMPLATE.md`), update `docs/BUILD_STATE.md` and this card's Status in `docs/BUILD_CALENDAR.md`.

If this card cannot be finished and verified in this run, mark it **Carried** — do not mark it Done.
