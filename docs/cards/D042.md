# D042 — Product sale path

**Build calendar:** [`docs/BUILD_CALENDAR.md`](../BUILD_CALENDAR.md#d042) · **Phase 2 — Catalog & Product Inventory** · Epic: Inventory
**Log for this card:** `docs/logs/D042.md` (created when this card is run)
**Gate this card contributes to:** —

## Task

Product sale path

## Deliverable

Sale service; server-side price computation

## Acceptance criteria

Client-supplied price is ignored and the server value used

## Security check

THREAT: price tampering. Prices never accepted from the client

## Prompt to run this card

Follow `CLAUDE.md` exactly. Read `docs/BUILD_STATE.md` first to confirm this is in fact the current card and that the tree is green. Then:

1. Write the failing test first (non-negotiable for domain, reservations, inventory, money, auth code).
2. Implement the smallest change that satisfies the acceptance criteria above.
3. Run the local gate (lint, typecheck, test, isolation suite where applicable).
4. Answer the security check above explicitly in the log entry.
5. Commit with `[D042]` in the message.
6. Write `docs/logs/D042.md` (use `docs/logs/TEMPLATE.md`), update `docs/BUILD_STATE.md` and this card's Status in `docs/BUILD_CALENDAR.md`.

If this card cannot be finished and verified in this run, mark it **Carried** — do not mark it Done.
