# D185 — Support action: resend notification / replay webhook event

**Build calendar:** [`docs/BUILD_CALENDAR.md`](../BUILD_CALENDAR.md#d185) · **Phase 11 — Platform Admin Console** · Epic: Support actions
**Log for this card:** `docs/logs/D185.md` (created when this card is run)
**Gate this card contributes to:** —
**Estimated build duration (AI-assisted session):** 1.5h — planning estimate, not a commitment; recalibrate against `docs/logs/D185.md` actual effort once run.

## Task

Support action: resend notification / replay webhook event

## Deliverable

Re-triggers a transactional notification or replays a stored webhook event for operational recovery; grant-gated

## Acceptance criteria

A resend/replay produces the expected downstream effect exactly once (idempotent) and is audit-logged

## Security check

Confirm a replay cannot double-apply a financial side effect (idempotency key reused, not regenerated)

## Prompt to run this card

Follow `CLAUDE.md` exactly. Read `docs/BUILD_STATE.md` first to confirm this is in fact the current card and that the tree is green. Read `docs/design/16_platform_admin_console.md` for the full context this card sits in before implementing. Then:

1. Write the failing test first (non-negotiable for domain, reservations, inventory, money, auth code — and for anything in Phase 11 touching `support_access_grants` or platform roles).
2. Implement the smallest change that satisfies the acceptance criteria above.
3. Run the local gate (lint, typecheck, test, isolation suite where applicable).
4. Answer the security check above explicitly in the log entry.
5. Commit with `[D185]` in the message.
6. Write `docs/logs/D185.md` (use `docs/logs/TEMPLATE.md`), update `docs/BUILD_STATE.md` and this card's Status in `docs/BUILD_CALENDAR.md`.

If this card cannot be finished and verified in this run, mark it **Carried** — do not mark it Done.
