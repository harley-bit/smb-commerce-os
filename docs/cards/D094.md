# D094 — Authorization via provider

**Build calendar:** [`docs/BUILD_CALENDAR.md`](../BUILD_CALENDAR.md#d094) · **Phase 5 — Payments — Stripe Connect** · Epic: Deposits and refunds
**Log for this card:** `docs/logs/D094.md` (created when this card is run)
**Gate this card contributes to:** —

## Task

Authorization via provider

## Deliverable

Manual-capture PaymentIntent for deposits

## Acceptance criteria

Hold placed; expiry recorded from provider response

## Security check

expires_at mandatory — drives the auto-release job

## Prompt to run this card

Follow `CLAUDE.md` exactly. Read `docs/BUILD_STATE.md` first to confirm this is in fact the current card and that the tree is green. Then:

1. Write the failing test first (non-negotiable for domain, reservations, inventory, money, auth code).
2. Implement the smallest change that satisfies the acceptance criteria above.
3. Run the local gate (lint, typecheck, test, isolation suite where applicable).
4. Answer the security check above explicitly in the log entry.
5. Commit with `[D094]` in the message.
6. Write `docs/logs/D094.md` (use `docs/logs/TEMPLATE.md`), update `docs/BUILD_STATE.md` and this card's Status in `docs/BUILD_CALENDAR.md`.

If this card cannot be finished and verified in this run, mark it **Carried** — do not mark it Done.
