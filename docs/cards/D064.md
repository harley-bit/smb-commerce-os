# D064 — Buffer expansion

**Build calendar:** [`docs/BUILD_CALENDAR.md`](../BUILD_CALENDAR.md#d064) · **Phase 4 — Rentals, Deposits & Returns** · Epic: Rental core
**Log for this card:** `docs/logs/D064.md` (created when this card is run)
**Gate this card contributes to:** —
**Estimated build duration (AI-assisted session):** 3.0h — planning estimate, not a commitment; recalibrate against `docs/logs/D064.md` actual effort once run.

## Task

Buffer expansion

## Deliverable

Interval expanded at write time; buffer stored on reservation

## Acceptance criteria

Later variant buffer change does not alter existing bookings

## Security check

Stored buffer prevents retroactive schedule mutation

## Prompt to run this card

Follow `CLAUDE.md` exactly. Read `docs/BUILD_STATE.md` first to confirm this is in fact the current card and that the tree is green. Then:

1. Write the failing test first (non-negotiable for domain, reservations, inventory, money, auth code).
2. Implement the smallest change that satisfies the acceptance criteria above.
3. Run the local gate (lint, typecheck, test, isolation suite where applicable).
4. Answer the security check above explicitly in the log entry.
5. Commit with `[D064]` in the message.
6. Write `docs/logs/D064.md` (use `docs/logs/TEMPLATE.md`), update `docs/BUILD_STATE.md` and this card's Status in `docs/BUILD_CALENDAR.md`.

If this card cannot be finished and verified in this run, mark it **Carried** — do not mark it Done.
