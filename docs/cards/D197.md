# D197 — Platform admin console review — GATE G11

**Build calendar:** [`docs/BUILD_CALENDAR.md`](../BUILD_CALENDAR.md#d197) · **Phase 11 — Platform Admin Console** · Epic: Feature flags & role management
**Log for this card:** `docs/logs/D197.md` (created when this card is run)
**Gate this card contributes to:** G11
**Estimated build duration (AI-assisted session):** 3.0h — planning estimate, not a commitment; recalibrate against `docs/logs/D197.md` actual effort once run.

## Task

Platform admin console review — GATE G11

## Deliverable

Full review of Epics A–E against 16_platform_admin_console.md: grant-scoping test suite, audit-row-per-action test, network-restriction verification

## Acceptance criteria

G11 test proves no merchant-data access succeeds without an active grant; every Epic B/C/D/E mutating action produces an audit row with a traceable actor; network restriction on apps/admin confirmed enforced, not just documented

## Security check

Confirm dual-control and MFA-for-all-platform-roles requirements are both actually tested, not just implemented

## Prompt to run this card

Follow `CLAUDE.md` exactly. Read `docs/BUILD_STATE.md` first to confirm this is in fact the current card and that the tree is green. Read `docs/design/16_platform_admin_console.md` for the full context this card sits in before implementing. Then:

1. Write the failing test first (non-negotiable for domain, reservations, inventory, money, auth code — and for anything in Phase 11 touching `support_access_grants` or platform roles).
2. Implement the smallest change that satisfies the acceptance criteria above.
3. Run the local gate (lint, typecheck, test, isolation suite where applicable).
4. Answer the security check above explicitly in the log entry.
5. Commit with `[D197]` in the message.
6. Write `docs/logs/D197.md` (use `docs/logs/TEMPLATE.md`), update `docs/BUILD_STATE.md` and this card's Status in `docs/BUILD_CALENDAR.md`.

If this card cannot be finished and verified in this run, mark it **Carried** — do not mark it Done.
