# D062 — Hold exhaustion limits — GATE G3

**Build calendar:** [`docs/BUILD_CALENDAR.md`](../BUILD_CALENDAR.md#d062) · **Phase 3 — Availability Engine & Services** · Epic: Services
**Log for this card:** `docs/logs/D062.md` (created when this card is run)
**Gate this card contributes to:** G3

## Task

Hold exhaustion limits — GATE G3

## Deliverable

Per-IP and per-account concurrent-hold caps

## Acceptance criteria

Anonymous cart cap enforced; abandonment monitored

## Security check

THREAT: DoS by reserving all inventory without paying

## Prompt to run this card

Follow `CLAUDE.md` exactly. Read `docs/BUILD_STATE.md` first to confirm this is in fact the current card and that the tree is green. Then:

1. Write the failing test first (non-negotiable for domain, reservations, inventory, money, auth code).
2. Implement the smallest change that satisfies the acceptance criteria above.
3. Run the local gate (lint, typecheck, test, isolation suite where applicable).
4. Answer the security check above explicitly in the log entry.
5. Commit with `[D062]` in the message.
6. Write `docs/logs/D062.md` (use `docs/logs/TEMPLATE.md`), update `docs/BUILD_STATE.md` and this card's Status in `docs/BUILD_CALENDAR.md`.

If this card cannot be finished and verified in this run, mark it **Carried** — do not mark it Done.
