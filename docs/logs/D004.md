# D004 — run log

**Build calendar:** [`docs/BUILD_CALENDAR.md`](../BUILD_CALENDAR.md#d004)
**Card:** [`docs/cards/D004.md`](../cards/D004.md)
**Run started (actual timestamp):** 2026-09-07 21:47 local
**Run ended (actual timestamp):** 2026-09-07 22:10 local
**Outcome:** Done

## What happened

Wired real Vitest coverage into `packages/domain`: added `vitest.config.ts` with the `v8` provider and `thresholds: { lines: 95, statements: 95, functions: 95, branches: 95 }` per `docs/design/10_secure_sdlc_ci_and_quality_gates.md`'s test-strategy table (domain unit tests at 95%+), added `@vitest/coverage-v8` as a devDependency, and changed the package's `test` script to `vitest run --coverage`.

Followed the non-negotiable failing-test-first rule for domain code: added a second function, `isNonEmptyLabel`, with an untested branch, ran `pnpm test` and confirmed the coverage gate correctly failed (44.44% statements/lines, 50% functions, all below the 95% threshold, non-zero exit). Then added tests for both branches, bringing coverage back to 100% and the gate green — proving the gate actually enforces the threshold rather than just being present in config.

While running the full local gate, found that the shared `@smb-os/eslint-config` package's ignore list (`dist/**`, `**/*.tsbuildinfo`, `.turbo/**`) didn't exclude `coverage/**`, so `pnpm lint` picked up 3 warnings from vitest's generated coverage HTML report in `packages/domain/coverage/`. Fixed by adding `coverage/**` to the shared ignore list in `packages/eslint-config/index.js` — this affects every package that will eventually run `--coverage`, not just domain, so it belongs in the shared config rather than a per-package override.

## Acceptance criteria check

- "`pnpm test` runs" — **met.** `pnpm test` at the repo root passes across all 7 workspace packages (10/10 turbo tasks).
- "coverage gate fails below threshold" — **met.** Directly verified: with `isNonEmptyLabel`'s false branch untested, `vitest run --coverage` exited non-zero with `ERROR: Coverage for lines (44.44%) does not meet global threshold (95%)` (and the same for functions/statements). After adding the missing test, coverage returned to 100% and the run passed.

## Security check answer

**Domain package threshold is set to 95%, enforced by `vitest.config.ts`'s `test.coverage.thresholds` (lines/statements/functions/branches all at 95), matching `docs/design/10_secure_sdlc_ci_and_quality_gates.md`'s test-strategy table** ("Domain unit | Vitest | ... | 95%+"). The domain package holds the invariants (state machines, conflict detection, money arithmetic per the design doc) and has no I/O, so there's no excuse for gaps — a threshold below 95% here would let an unreviewed invariant ship untested. The gate is real, not decorative: it was verified to actually fail the build (non-zero exit) when coverage dropped below 95%, not just configured and assumed to work.

## Gate check (if this day ends a phase)

N/A — D004 does not end a phase gate (Phase 0 continues through D012).

## Commit(s)

`b7f7998 feat(test): add Vitest coverage thresholds with domain package at 95% [D004]`

## Carried / blocked reason (if not Done)

N/A — Done.

## Notes for next session

Local gate (`pnpm lint && pnpm typecheck && pnpm test && pnpm test:isolation`) is green, 10/10 tasks across all 7 workspace projects. Only `packages/domain` has real coverage enforcement so far — other packages (`config`, `contracts`, `eslint-config`) still run plain `vitest run` with no threshold, which is fine for now since they're tooling/config packages, not domain/reservation/money/auth code; revisit if the secure-SDLC doc's 80%+ integration-layer target becomes relevant once `packages/db` and `apps/api` grow real logic. `test:isolation` is still a stub `echo` everywhere — that's correct for now, since no repository/tenant-scoped code exists yet to isolate-test; it becomes real once Phase 1+ introduces `packages/db` repositories, not as part of this card's scope. Next up: D005 (CI pipeline skeleton).
