# D008 — run log

**Build calendar:** [`docs/BUILD_CALENDAR.md`](../BUILD_CALENDAR.md#d008)
**Card:** [`docs/cards/D008.md`](../cards/D008.md)
**Run started (actual timestamp):** 2026-09-07 22:10 local
**Run ended (actual timestamp):** 2026-09-07 22:20 local
**Outcome:** Done

## What happened

Installed `semgrep` (1.176.0, via `brew install semgrep`) as a third local machine dependency alongside gitleaks and syft, and wrote `packages/config/test/sast.test.ts` test-first: it plants a real command-injection fixture (`child_process.exec` with unsanitized input, tripping the real `javascript.lang.security.detect-child-process` rule in the `p/security-audit` registry ruleset) and asserts `semgrep scan --config p/security-audit --error` exits non-zero and reports a "blocking" finding, plus a clean-fixture control that must exit 0, a scan of this repo's own source tree (0 findings), and a workflow-ordering check. Confirmed the CI-ordering assertion failed first (semgrep wasn't in `ci.yml` yet), then wired a `Static analysis (semgrep)` step into `.github/workflows/ci.yml` (installed via `pip install --break-system-packages semgrep==1.176.0` on the `ubuntu-latest` runner, after `pnpm audit` and before lint/test), which turned all 4 tests green. Full local gate (`lint`, `typecheck`, `test`, `test:isolation`) passes.

## Acceptance criteria check

- "Build fails on high/critical finding" — **Met.** `semgrep scan --config p/security-audit --error` exits 1 on a fixture with a real blocking finding (verified directly and via the regression test); the same command is wired into CI immediately after the dependency-vulnerability-scan step and before lint/typecheck/test, so a real finding in a PR would fail the job before any other check runs.

## Security check answer

**Security check:** "Static analysis before any application code exists."
**Answer:** Correct call, and this card follows through on it — no application code exists yet (Phase 0 is tooling/CI only), so wiring SAST now means every line of `packages/domain`, `apps/api`, etc. written from D010 onward is scanned from its first commit rather than retrofitted after a vulnerable pattern has already shipped. The `p/security-audit` ruleset is generic (not TS/JS-specific only) so it also covers the Python/YAML/shell fixtures and CI scripts already in the repo.

## Gate check (if this day ends a phase)

N/A — D008 does not end a phase (Phase 0 gate is G0, at D012).

## Commit(s)

f4d6c8f feat(security): add semgrep SAST scanning to CI [D008] (recorded below at actual commit time)

## Carried / blocked reason (if not Done)

N/A — Done.

## Notes for next session

- `semgrep` (1.176.0, `brew install semgrep`) is now a **third local machine dependency** (alongside gitleaks and syft): `packages/config/test/sast.test.ts` shells out to the real binary, so it must be on `PATH` for `pnpm test` to pass on any machine. CI installs it via `pip install --break-system-packages semgrep==1.176.0` on `ubuntu-latest` (PEP 668 externally-managed-environment flag required on that runner's Python).
- Ruleset choice: `p/security-audit` (Semgrep Registry, no login required, no metrics-sensitive `auto` config). Scans this repo's own tree clean (0 findings) as of this card. If a future card adds TS/JS-specific coverage, consider layering `p/javascript`/`p/typescript` packs too — not done here since `p/security-audit` already satisfied the card's acceptance criteria and the current tree is clean under it.
- CI step order is now: checkout → corepack → node/pnpm setup → install gitleaks → `pnpm install --frozen-lockfile` → `pnpm audit --audit-level=high` → install semgrep → `semgrep scan ... --error` → lint → typecheck → test → test:isolation → secret scan (gitleaks) → install syft → SBOM generate/upload. As with D005–D007, this ordering has only been verified by running the exact command sequence locally, not via a live GitHub Actions run (no remote push access in this session) — carries forward the same "first real push should confirm Actions is green" note from prior logs.
- Next unblocked card: **D009 — Branch protection and PR template.**
