# D001 — run log

**Build calendar:** [`docs/BUILD_CALENDAR.md`](../BUILD_CALENDAR.md#d001)
**Card:** [`docs/cards/D001.md`](../cards/D001.md)
**Run started (actual timestamp):** 2026-09-07 21:34 local
**Run ended (actual timestamp):** 2026-09-07 21:40 local
**Outcome:** Done

## What happened

Initialized the pnpm workspace + Turborepo monorepo per ADR-001's repository layout: `apps/{api,web}` and `packages/{domain,db,contracts,config}`, each with a minimal `package.json` and `tsconfig.json`. Wired real internal dependency edges (`@smb-os/web` → `@smb-os/contracts`; `@smb-os/api` → `@smb-os/db` → `@smb-os/domain`) so `pnpm install` and `turbo run build` had an actual graph to resolve, not just empty leaf packages. `packages/domain` got a Vitest unit test (`workspaceGraphMarker`) as the first package under the `/packages/domain` no-I/O rule, since D001 has no domain/reservations/inventory/money/auth logic of its own to test-first — the acceptance criterion itself (`pnpm install` succeeds, workspace graph resolves) was verified directly via `pnpm -r list --depth -1` and `turbo run build --dry=json`, both of which confirm all 6 internal packages and their dependency ordering. One surprise: running `pnpm install` before any `package.json` existed silently auto-generated a throwaway root `package.json`/`pnpm-lock.yaml` (pnpm 12 default-init behavior) — deleted before building the real scaffold so it wouldn't get committed.

## Acceptance criteria check

- `pnpm install` succeeds — **met.** Clean install, 47 packages resolved, `ERR_PNPM_IGNORED_BUILDS` for `esbuild` (vitest's transitive dep) resolved by explicitly approving it in `pnpm-workspace.yaml`'s `allowBuilds`.
- Workspace graph resolves — **met.** `pnpm -r list --depth -1` lists all 7 projects (root + 6 members); `turbo run build --dry=json` shows the correct internal dependency graph; `pnpm build` actually compiles all 6 packages successfully via their real import edges.

## Security check answer

**No secrets in initial commit; .gitignore covers .env** — confirmed. `.gitignore` already had `.env`/`.env.local`; added `*.tsbuildinfo` (TypeScript build cache, harmless but shouldn't be versioned) before staging. Reviewed the full `git status`/staged diff before committing: only source scaffolding, `package.json`/`tsconfig.json` files, `pnpm-lock.yaml` (dependency resolution only, no credentials), and config — no `.env`, no keys, no tokens.

## Gate check (if this day ends a phase)

N/A — D001 does not end a phase gate (G0 is later in Phase 0).

## Commit(s)

`6f8115b feat(monorepo): initialize pnpm workspaces + Turborepo per ADR-001 [D001]`

## Carried / blocked reason (if not Done)

N/A — Done.

## Notes for next session

Local gate (`pnpm lint && pnpm typecheck && pnpm test && pnpm test:isolation`) is green, but `lint`/`test`/`test:isolation` are currently stub `echo` scripts in every package except `packages/domain`'s real Vitest run — D003 (lint/format rules) and D004 (test harness) are what actually wire these up repo-wide, not D001. Next card is D002 — TypeScript strict configuration (the base `packages/config/tsconfig.base.json` already has `strict: true` and a few extra checks, but D002 should own hardening/auditing that config properly rather than treating this as done).
