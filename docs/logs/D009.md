# D009 — run log

**Build calendar:** [`docs/BUILD_CALENDAR.md`](../BUILD_CALENDAR.md#d009)
**Card:** [`docs/cards/D009.md`](../cards/D009.md)
**Run started (actual timestamp):** 2026-09-07 22:12 local
**Run ended (actual timestamp):** 2026-09-07 22:31 local
**Outcome:** Carried

## What happened

Confirmed the tree was green (`pnpm lint/typecheck/test/test:isolation`, 10/10 tasks, all cached
or passing) before starting. Built the two pieces of this card that are actually implementable and
verifiable inside this repo without a GitHub remote: `.github/pull_request_template.md` with a
self-review checklist, and a Conventional Commits `commit-msg` hook (`.githooks/commit-msg`,
installed via the existing `prepare` → `git config core.hooksPath .githooks` wiring from D006).
Both were built test-first (`packages/config/test/pr-template.test.ts` greps the template for the
checklist/CC8/gate-step language; `packages/config/test/commit-message-format.test.ts` shells out
to the real hook script against valid and invalid commit messages, mirroring the D006/D007/D008
pattern of testing the real artifact, not a mock). The hook was also verified end-to-end for real:
this card's own commit ran through it live via the repo's already-active `core.hooksPath`, and it
accepted the conventional-format message.

The remaining piece of the deliverable — actual **branch protection rules** rejecting a direct push
to `main` — could not be done. This repo has no GitHub remote configured (`.git/config` has no
`[remote]` section) and `gh` is not installed in this environment, and CLAUDE.md's WHERE section
explicitly excludes remote git operations and any change to shared/remote infrastructure from
things this session can do unilaterally — branch protection is a GitHub repo *setting*, not a file
in this tree, and there is nothing to configure it against yet. There is also no local equivalent
to fake this the way D005 faked "CI green" by running the same commands locally: a direct push to
`main` literally requires a remote to reject it against.

## Acceptance criteria check

- "Direct push to main rejected" — **NOT met.** No GitHub remote exists for this repo yet, and no
  `gh`/API access is available in this sandboxed session to configure a ruleset even if one did.
  This is an external/account-provisioning blocker per `CLAUDE.md`'s protocol, not a design gap:
  the PR template and commit-msg pieces of the deliverable are done and tested; the branch
  protection rule itself needs a human to either provision the GitHub remote + admin access, or to
  apply the ruleset by hand once one exists.

## Security check answer

"Self-review checklist is the CC8 change-management evidence" — answered by building
`.github/pull_request_template.md` so that the self-review checklist is the artifact a reviewer
(or the author, solo) checks off before merge, and stating explicitly in the template itself that
this checklist *is* the CC8 evidence, not a separate compliance document to keep in sync. Each item
maps to a concrete, checkable gate (lint/typecheck/test/isolation, dependency justification, no
secrets/card data, server-side price recomputation, Conventional Commits) rather than a vague
"reviewed it" checkbox, so the evidence trail is the same artifact as the checklist a human would
actually use.

## Commit(s)

`fc3545d feat(ci): add PR template with self-review checklist and commit-msg hook [D009]`

## Carried / blocked reason (if not Done)

Blocked on external provisioning: this repo has no GitHub remote and this sandboxed session has no
`gh`/GitHub API access, so the "direct push to main rejected" acceptance criterion has no remote to
enforce against and cannot be verified. PR template and commit-msg enforcement (the two locally
buildable parts of the deliverable) are done and tested.

## Notes for next session

D009 stays the current card. Once a GitHub remote exists for this repo and a human has `gh` (or
web UI) admin access, apply a branch protection ruleset on `main`: require the CI status checks
(lint/typecheck/test/test:isolation) to pass, require at least one PR review before merge (or, for
a solo maintainer, "require a pull request before merging" with self-review), block force-pushes,
and disallow direct pushes. Once that's applied, re-run this card's acceptance check against a real
push attempt to confirm it's actually rejected, then mark D009 Done and advance the pointer. Until
then, the next unblocked card in Phase 0 is D010 (native auth implementation per ADR-002) —
consider running that instead of idling on D009, per CLAUDE.md's guidance for external blockers,
though the human operator directed this session specifically to leave the pointer on D009 rather
than advance it, so that is deferred to the next explicit instruction.
