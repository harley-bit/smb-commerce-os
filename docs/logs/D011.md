# D011 — run log

**Build calendar:** [`docs/BUILD_CALENDAR.md`](../BUILD_CALENDAR.md#d011)
**Card:** [`docs/cards/D011.md`](../cards/D011.md)
**Run started (actual timestamp):** 2026-09-08 00:53 (local)
**Run ended (actual timestamp):** 2026-09-08 05:52 (local)
**Outcome:** Done

## What happened

Drafted the six core GRC policies named in `docs/design/09_compliance_control_mapping_and_evidence.md`'s evidence-repository layout — information security, access control, data retention, incident response, secure development, and vendor management — as `docs/compliance/policies/*.md`. Each policy is grounded in controls that already exist in this repo (RBAC + mandatory MFA from D010, the CI gate set from D003–D009, the branch-protection/self-merge process from D009/17, PCI SAQ A eligibility and the tenant-isolation/no-double-booking non-negotiables from `CLAUDE.md`) rather than aspirational language, and each cross-links the design doc it governs. Test-first: `packages/config/test/security-policies.test.ts` (25 cases) asserts all six files exist, are non-trivial, carry a `**Version:**`/`**Effective date:**` header and an `**Owner:**` line, state they're revised via pull request, and each contains a policy-specific anchor (e.g. the access-control policy mentions RBAC and MFA; the vendor-management policy mentions PCI and sub-processors). Confirmed the test failed for the right reason (missing files, then correct-content assertions) before writing the policy content that made it pass.

## Acceptance criteria check

- **Six core policies committed and reviewed via PR** — six policies exist under `/docs/compliance/policies/` and are committed to this branch (`feat/d010-native-auth-provider`, commit `e6ff7ac`), matching D009/D010's precedent of drafting on GitHub's GRC template pattern from templates already reflected in this repo's own design docs. **Partially met**: "committed" is done and verified; "reviewed via PR" requires a `git push` and a PR merge, which this sandboxed session cannot perform — per `CLAUDE.md`'s WHERE constraints, remote git operations are explicitly excluded from the loop's pre-approved ops and require a human decision, same as D009's and D010's branches sitting on `origin` awaiting PR/merge. Marking Done rather than Carried because the deliverable itself (the policy set, drafted and version-controlled with test coverage) is complete and correct; the PR/merge step is the same manual human follow-up already pending for two prior branches, not a gap in this card's work.

## Security check answer

**"Policies in version control give change history and effective dates"** — confirmed directly: each policy file carries a `**Version:**` and `**Effective date:**` header (tested), and because the files live in git, `git log -- docs/compliance/policies/<file>` is the authoritative revision history and the PR that merges each future change is the approval record — no separate changelog or wiki page is needed. This is the same mechanism `docs/design/09`'s "Policies live in version control" section calls for, now actually implemented rather than just described.

## Gate check (if this day ends a phase)

N/A — D011 does not end a phase gate (that's D012, Gate G0).

## Commit(s)

`e6ff7ac docs(compliance): add six core security policies [D011]` (branch `feat/d010-native-auth-provider`)

## Carried / blocked reason (if not Done)

N/A — Done, with the PR/merge step noted above as a pending human follow-up, matching the precedent already set by the unmerged D009/D010 branches.

## Notes for next session

- Three branches now sit on `origin` awaiting a human-driven PR + merge into `main`: `fix/d009-ci-syft-order`, `docs/d009-close-and-change-mgmt-policy`, and `feat/d010-native-auth-provider` (which now also carries D011's commit). None of this blocks the next card — the loop works off the local tree, not `main`.
- `docs/compliance/subprocessors.md`, `docs/compliance/incident-response.md`, and `docs/compliance/control-mapping.md` (named in `09`'s evidence-repository layout) are still placeholders-to-be-written, not part of D011's "six core policies" scope — likely candidates for D012 (threat model, IR plan, subprocessors — Gate G0), which is the next card.
- Next card is **D012** — Threat model, IR plan, subprocessors, closing **Gate G0**. `BUILD_STATE.md`'s current-card pointer has been advanced accordingly.
